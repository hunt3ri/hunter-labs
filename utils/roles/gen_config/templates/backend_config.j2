# Terraform backend config for {{ aws_environment }} environment.  See https://www.terraform.io/docs/language/settings/backends/configuration.html#partial-configuration
# Command to execute:
#    terraform init --backend-config=/tools/terraform/config/backend_{{ aws_environment }}.config

region = "{{ aws_region }}"
profile = "{{ aws_profile }}"
bucket = "{{ terraform_bucket }}"
